services:
  db:
    container_name: job_application_tracker
    image: postgres:latest
    environment:
      POSTGRES_USER: youmans
      POSTGRES_PASSWORD: password
      POSTGRES_DB: job_application_tracker
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    ports:
      - "5332:5432"
    networks:
      - db
    restart: unless-stopped

networks:
  db:
    driver: bridge

volumes:
  db:
    driver: local